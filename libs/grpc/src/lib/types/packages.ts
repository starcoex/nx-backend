export enum Packages {
  AUTH = 'auth',
  NOTIFICATIONS = 'notifications',
  PAYMENTS = 'payments',
  RESERVATIONS = 'reservations',
  USERS = 'users',
  JOBS = 'jobs',
  PRODUCTS = 'products',
  UPLOADS = 'uploads',
}
